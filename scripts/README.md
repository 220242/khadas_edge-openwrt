# scripts

+ `build` - build openwrt image
+ `write2sd_card` - write image to sd card
+ `pkg` - package manager wrapper (apk for OpenWrt 25.x+, opkg for 23.05/24.10)
+ `opkg` - compat alias for `pkg`
